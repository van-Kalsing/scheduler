function render_table_calendar(interface_control) {
	interface_control.set_add_work_status(true);
	
	var container_element = interface_control.get_container();
	
	container_element.html("Здесь будет табличный календарь");
}
